{
"name":"filesystem",

"description":"filesystem is made to hold checks about detected filesystems",

"path":"/syleps",

"macros":{
	 "_SNMPCOMMUNITY": {"type":"string",
	                     "description":"The read snmp community allowed on the linux server"
			     },
         "_FS_PATTERN": {"type":"regex",
                             "description":"regex pattern to match fs desc in snmp table."
                             },

         "_FS_WARN": {"type":"integer",
                             "description": "Value for starting warning state for filesystem usage."
                             },
         "_FS_CRIT": {"type":"string",
                             "description": "Value for starting critical state for filesystem usage"
                             }
}


}

define host{
        use                     linux,generic-host
        host_name               localhost
        address                 localhost
	contact_groups		admins

        _CHKLOG_CONF            $PLUGINSDIR$/logFiles_localhost.conf
}

